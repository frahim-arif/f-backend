const Question = require("../../models/question.model");

// =================== ADD NEW QUESTION ===================
exports.addQuestion = async (req, res) => {
  try {
    const newQuestion = await Question.create({
      category: req.body.category,
      question: req.body.question,
      answer: req.body.answer,
      hawala1: req.body.hawala1,
      hawala2: req.body.hawala2,
      hawala3: req.body.hawala3,
    });

    res.status(201).json({
      success: true,
      message: "Question added successfully",
      data: newQuestion,
    });
  } catch (err) {
    res.status(500).json({
      success: false,
      message: err.message,
    });
  }
};

// =================== GET ALL QUESTIONS ===================
exports.getQuestions = async (req, res) => {
  try {
    const questions = await Question.find().sort({ createdAt: -1 });

    res.status(200).json({
      success: true,
      data: questions,
    });
  } catch (err) {
    res.status(500).json({
      success: false,
      message: err.message,
    });
  }
};


const Question = require("../../models/question.model");

// =================== ADD NEW QUESTION ===================
exports.addQuestion = async (req, res) => {
  try {
    const newQuestion = await Question.create({
      category: req.body.category,
      question: req.body.question,
      answer: req.body.answer,
      hawala1: req.body.hawala1,
      hawala2: req.body.hawala2,
      hawala3: req.body.hawala3,
      // slug automatically generated by pre-save hook in model
    });

    res.status(201).json({
      success: true,
      message: "Question added successfully",
      data: newQuestion,
    });
  } catch (err) {
    res.status(500).json({
      success: false,
      message: err.message,
    });
  }
};

// =================== GET ALL QUESTIONS ===================
exports.getQuestions = async (req, res) => {
  try {
    const limit = parseInt(req.query.limit) || 5;
    const skip = parseInt(req.query.skip) || 0;

    const questions = await Question.find()
      .sort({ createdAt: -1 })
      .skip(skip)
      .limit(limit);

    res.status(200).json({
      success: true,
      data: questions,
    });
  } catch (err) {
    res.status(500).json({
      success: false,
      message: err.message,
    });
  }
};

// =================== GET QUESTION BY SLUG ===================
exports.getQuestionBySlug = async (req, res) => {
  try {

    let slug = decodeURIComponent(req.params.slug)
      .trim()
      .toLowerCase();

    const question = await Question.findOne({
      $or: [
        { slug: slug },
        { slug: new RegExp(`^${slug}$`, "i") },
        { oldSlugs: slug }
      ]
    });

    if (!question) {
      return res.status(404).json({
        success: false,
        message: "Question not found",
      });
    }

    return res.status(200).json({
      success: true,
      data: question,
    });

  } catch (err) {
    return res.status(500).json({
      success: false,
      message: err.message,
    });
  }
};

// =================== GET QUESTIONS BY CATEGORY ===================
exports.getQuestionsByCategory = async (req, res) => {
  try {
    const category = req.params.category;
    const limit = parseInt(req.query.limit) || 5;
    const skip = parseInt(req.query.skip) || 0;

    const questions = await Question.find({ category })
      .sort({ createdAt: -1 })
      .skip(skip)
      .limit(limit);

    res.status(200).json({
      success: true,
      data: questions,
    });
  } catch (err) {
    res.status(500).json({
      success: false,
      message: err.message,
    });
  }
};
